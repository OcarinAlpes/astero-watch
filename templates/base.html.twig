<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}AsteroWatch{% endblock %}</title>
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .dangerous { color: #dc3545; font-weight: bold; }
        .safe { color: #198754; }
        .neo-card { transition: transform 0.2s; }
        .neo-card:hover { transform: translateY(-5px); }
    </style>
    {% block stylesheets %}{% endblock %}
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
        <div class="container">
            <a class="navbar-brand" href="{{ path('neo_index') }}">🚀 AsteroWatch</a>
            <div class="navbar-nav">
                <a class="nav-link" href="{{ path('neo_index') }}">Aujourd'hui</a>
                <a class="nav-link" href="{{ path('neo_browse') }}">Explorer</a>
                <a class="nav-link" href="{{ path('favorite_index') }}">⭐ Favoris</a>
            </div>
        </div>
    </nav>

    <main class="container">
        {% for message in app.flashes('error') %}
            <div class="alert alert-danger">{{ message }}</div>
        {% endfor %}
        {% for message in app.flashes('success') %}
            <div class="alert alert-success">{{ message }}</div>
        {% endfor %}

        {% block body %}{% endblock %}
    </main>

    <footer class="container mt-5 py-3 text-center text-muted">
        <small>Données fournies par <a href="https://api.nasa.gov/">NASA API</a></small>
    </footer>

    {% block javascripts %}{% endblock %}
</body>
</html>